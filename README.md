📊 Student Marks Analysis (Minor II Project)

This project analyzes student marks using Python and Pandas.
It reads student data from a CSV file, calculates average, maximum, and minimum marks, compares students using bar charts, and generates additional insights like percentage, grades, and results.

Technologies Used
  Python
  Pandas
  Matplotlib


Step 1: 
Install Python
Windows

Download Python from: https://www.python.org/downloads/

Check “Add Python to PATH”

Verify installation:

python --version


Linux (Ubuntu/Debian)
sudo apt update
sudo apt install python3 python3-venv python3-pip
python3 --version


Step 2: 
Create Virtual Environment (venv)
Windows
python -m venv venv
venv\Scripts\activate

Linux
python3 -m venv venv
source venv/bin/activate

Step 3: 
Install Required Libraries
pip install pandas matplotlib
